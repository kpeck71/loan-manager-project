import React from 'react';

const CompletedGoals = props => {
  // renderGoals = props => {
  //   this.props.goals.filter((goal)=> goal.paid === true)
  // <Goal goal={goal} />
  // }
    return (
      <div className="CompletedGoals">
        <h2>Here is where completed goals will go:</h2>
      </div>
    );
  }

export default CompletedGoals
