import React from 'react';

const ExpensesView = (props) => {
  return (
  <div>
    <h3>Expenses:</h3>
    <br></br>
   {/* props.expenses.map((expense) => { return expense.expense_name} ) */}
  </div>
  )

}
export default ExpensesView;
